@model Data.StockManagement.Recommendation


<h3>Editar recomendacion de producto</h3>

@using (Html.BeginForm("Edit", "Recommendations", new { @id = '/' }, FormMethod.Post))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.RecommendationnId)
        <div class="form-group">
            <div class="col-md-10">
                @Html.EditorFor(model => model.RecommendationDescription, new { htmlAttributes = new { @class = "form-control", @id = "Recommendation-editor" } })
                @Html.ValidationMessageFor(model => model.RecommendationDescription, "", new { @class = "text-danger" })
            </div>
        </div>

            <div class="form-group text-lg-right">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Editar" class="btn pull-right btn-warning" />
                </div>
            </div>
    </div>
}