@model Data.StockManagement.Part


<h3>Editar pieza</h3>

@using (Html.BeginForm("Edit", "Parts", new { @id = '/' }, FormMethod.Post))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.PartId)
        <div class="form-group">
            <div class="col-md-10">
                @Html.EditorFor(model => model.PartDescription, new { htmlAttributes = new { @class = "form-control", @id = "part-editor" } })
                @Html.ValidationMessageFor(model => model.PartDescription, "", new { @class = "text-danger" })
            </div>
        </div>

            <div class="form-group text-lg-right">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Editar" class="btn pull-right btn-warning" />
                </div>
            </div>
    </div>
}